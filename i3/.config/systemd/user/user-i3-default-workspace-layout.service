[Unit]
Description=Sets the i3 default workspace layouts
PartOf=user-session.target

[Service]
Type=simple
Restart=always
ExecStart=%h/.bin/i3-default-workspace-layout

[Install]
WantedBy=user-session.target
